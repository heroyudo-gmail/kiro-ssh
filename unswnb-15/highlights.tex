%% Highlights for JISA/Elsevier submission.
%% Submit as a SEPARATE item ("Highlights") per Elsevier rules.
%% Rules: 3-5 bullet points; each <= 85 characters (including spaces).
%% Compile standalone or paste the bullet text into the submission system.

\documentclass{article}
\usepackage[margin=2.5cm]{geometry}
\begin{document}

\section*{Highlights}
\begin{itemize}
  \item Single-source NIDS collapse cross-network (MCC 0.9 to about 0).
  \item Semantic Feature Mapping exposes a real cross-tool extractor mismatch.
  \item Joint training shows shared features are sufficiently discriminative.
  \item Only 1\% target labels recover cross-network MCC to 0.65--0.91.
  \item Validated on real AWS traffic; benefit depends on evaluation protocol.
\end{itemize}

\end{document}
